; Full coverage INI file
# Both comment styles

[section] ; inline comment after section
[another.section]  # inline comment with hash

; Escaped characters in quoted strings
key1 = "string with \"escaped quotes\""
key2 = 'single quoted with \'escaped quotes\''
key3 = "backslash \\ and newline \n"

; Inline comments after values
value1 = test ; this is a comment
value2 = 123 # another comment

; Environment variables
unix_var = ${HOME}/path
windows_var = %USERPROFILE%\path
mixed = ${VAR1}/and/%VAR2%/path

; Null values
empty =
null_val = null
none_val = none

; Hex numbers
hex1 = 0xFF
hex2 = 0x1a2b3c

; Whitespace handling
  indented_key = value
key_with_spaces   =   value_with_spaces

; Key without value
standalone_key