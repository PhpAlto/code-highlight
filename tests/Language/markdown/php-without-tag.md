# PHP Code Without Opening Tag

## Code Block Without `<?php`

```php
function hello($name) {
    return "Hello, " . $name;
}
```

## Another Example

```php
$users = ['Alice', 'Bob', 'Charlie'];
foreach ($users as $user) {
    echo $user . "\n";
}
```
