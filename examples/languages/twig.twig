{% extends 'base.html.twig' %}
{% block body %}
  {% set users = users|filter(user => user.active) %}
  <ul class="users">
  {% for user in users %}
    <li>{{ user.name|upper }}</li>
  {% endfor %}
  </ul>{% endblock %}
